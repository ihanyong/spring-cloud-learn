server:
  port: 8010
spring:
  application:
    name: consumer-user-ribbon
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    preferIpAddress: true

spring.zipkin.baseUrl: http://localhost:9411


logging.level.org.springframework.cloud.sleuth.zipkin.ZipkinSpanListener: DEBUG